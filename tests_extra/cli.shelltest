$ burn -v
	* stdout
		Burn 0.1

$ burn --version
	* stdout
		Burn 0.1

$ burn -h | head -n1 | grep "^usage:"
	* exit status zero

$ burn --help | head -n1 | grep "^usage:"
	* exit status zero

stdin.burn:
	print "Hello world!"

$ burn - < stdin.burn
	* stdout
		Hello world!
