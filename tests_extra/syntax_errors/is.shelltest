err.burn:
	is Integer

$ burn err.burn
	* stderr
		Expected expression.
		in err.burn on line 1
			is Integer
			^
	* exit status nonzero

err.burn:
	3 is

$ burn err.burn
	* stderr
		Expected expression.
		in err.burn on line 1
			3 is
			    ^
	* exit status nonzero

err.burn:
	is

$ burn err.burn
	* stderr
		Expected expression.
		in err.burn on line 1
			is
			^
	* exit status nonzero
