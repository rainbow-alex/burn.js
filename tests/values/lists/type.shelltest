permanent.burn:
	print List is Type.Permanent

$ $BURN permanent.burn

type.burn:
	assert( [] is List )
	assert( [ 1, 2, 3 ] is List )

$ $BURN type.burn

safe.burn:
	import burn.types.Safe
	assert( [] is not Safe )

$ $BURN safe.burn
