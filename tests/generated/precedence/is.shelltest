// generated by `etc/generate_precedence_test.py is`

// GREP_ME precedence is or

// GREP_ME precedence is and

// GREP_ME precedence is not

is_is.burn:
	nothing is nothing is nothing
$ $BURN is_is.burn
	* exit status nonzero

is_is_not.burn:
	nothing is nothing is not nothing
$ $BURN is_is_not.burn
	* exit status nonzero

is_eq.burn:
	nothing is nothing == nothing
$ $BURN is_eq.burn
	* exit status nonzero

is_neq.burn:
	nothing is nothing != nothing
$ $BURN is_neq.burn
	* exit status nonzero

is_lt.burn:
	nothing is nothing < nothing
$ $BURN is_lt.burn
	* exit status nonzero

is_gt.burn:
	nothing is nothing > nothing
$ $BURN is_gt.burn
	* exit status nonzero

is_lteq.burn:
	nothing is nothing <= nothing
$ $BURN is_lteq.burn
	* exit status nonzero

is_gteq.burn:
	nothing is nothing >= nothing
$ $BURN is_gteq.burn
	* exit status nonzero

// GREP_ME precedence is add

// GREP_ME precedence is sub

// GREP_ME precedence is mul

// GREP_ME precedence is div

// GREP_ME precedence is union

// GREP_ME precedence is intersection
