// generated by `etc/generate_precedence_test.py div`

// GREP_ME precedence div or

div_and.burn:
	import test
	assert( ( ( 2 / 2 ) and nothing ) == nothing )
	test.assert_throws( function() { 2 / ( 2 and nothing ) }, TypeError )
	assert( ( 2 / 2 and nothing ) == nothing )
	
$ $BURN div_and.burn

// GREP_ME precedence div not

// GREP_ME precedence div is

// GREP_ME precedence div is_not

// GREP_ME precedence div eq

// GREP_ME precedence div neq

div_lt.burn:
	import test
	assert( ( ( 0 / 2 ) < 0 ) == false )
	test.assert_throws( function() { 0 / ( 2 < 0 ) }, TypeError )
	assert( ( 0 / 2 < 0 ) == false )
	
$ $BURN div_lt.burn

div_gt.burn:
	import test
	assert( ( ( 0 / 2 ) > 0 ) == false )
	test.assert_throws( function() { 0 / ( 2 > 0 ) }, TypeError )
	assert( ( 0 / 2 > 0 ) == false )
	
$ $BURN div_gt.burn

div_lteq.burn:
	import test
	assert( ( ( 0 / 2 ) <= 0 ) == true )
	test.assert_throws( function() { 0 / ( 2 <= 0 ) }, TypeError )
	assert( ( 0 / 2 <= 0 ) == true )
	
$ $BURN div_lteq.burn

div_gteq.burn:
	import test
	assert( ( ( 0 / 2 ) >= 0 ) == true )
	test.assert_throws( function() { 0 / ( 2 >= 0 ) }, TypeError )
	assert( ( 0 / 2 >= 0 ) == true )
	
$ $BURN div_gteq.burn

// GREP_ME precedence div add

// GREP_ME precedence div sub

// GREP_ME precedence div mul

// GREP_ME precedence div div

// GREP_ME precedence div union

// GREP_ME precedence div intersection
