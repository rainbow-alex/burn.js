strings.burn:
	print "foo"
	print "ぎ"

$ $BURN strings.burn
	* stdout
		foo
		ぎ

escape.burn:
	print "foo\\bar"
	print "foo\nbar"
	print "foo\tbar"
// TODO more escape codes, unicode etc.

$ $BURN escape.burn
	* stdout
		foo\bar
		foo
		bar
		foo	bar

// TODO single quoted strings
