main_hello.burn:
	main( function( $process ) {
		print "hello"
	} )

$ $BURN main_hello.burn
	* stdout
		hello

main_type.burn:
	assert.throws( function() {
		main( 3 )
	}, ArgumentError )

$ $BURN main_type.burn
