// generated by `etc/generate_operator_test.py precedence is`

is_or.burn:
	try { print ( true is false ) or Something } catch TypeError $e { print "TypeError" }
	print true is ( false or Something )
	try { print true is false or Something } catch TypeError $e { print "TypeError" }

$ burn --tolerant is_or.burn
	* stdout
		TypeError
		true
		TypeError

is_and.burn:
	try { print ( true is true ) and Something } catch TypeError $e { print "TypeError" }
	print true is ( true and Something )
	try { print true is true and Something } catch TypeError $e { print "TypeError" }

$ burn --tolerant is_and.burn
	* stdout
		TypeError
		true
		TypeError
