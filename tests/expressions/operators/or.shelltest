// generated by `etc/generate_operator_test.py or`

or_result_1.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = nothing or nothing
	assert( $v0 is Nothing )
	
	let $v1 = nothing or true
	assert( $v1 is Boolean )
	assert( $v1 == true )
	
	let $v2 = nothing or false
	assert( $v2 is Boolean )
	assert( $v2 == false )
	
	let $v3 = nothing or 0
	assert( $v3 is Integer )
	assert( $v3 == 0 )
	
	let $v4 = nothing or 2
	assert( $v4 is Integer )
	assert( $v4 == 2 )
	
	let $v5 = nothing or -2
	assert( $v5 is Integer )
	assert( $v5 == -2 )
	
	let $v6 = nothing or 0.0
	assert( $v6 is Float )
	assert( ( -0.0001 < $v6 ) and ( $v6 < 0.0001 ) )
	
	let $v7 = nothing or 2.0
	assert( $v7 is Float )
	assert( ( 1.9999 < $v7 ) and ( $v7 < 2.0001 ) )
	
	let $v8 = nothing or -0.5
	assert( $v8 is Float )
	assert( ( -0.5001 < $v8 ) and ( $v8 < -0.4999 ) )
	
	let $v9 = nothing or "c"[0]
	assert( $v9 is Character )
	assert( $v9 == "c"[0] )
	
	let $v10 = nothing or ""
	assert( $v10 is String )
	assert( $v10 == "" )
	
	let $v11 = nothing or "apple"
	assert( $v11 is String )
	assert( $v11 == "apple" )
	
	let $v12 = nothing or "banana"
	assert( $v12 is String )
	assert( $v12 == "banana" )
	
	let $v13 = nothing or repr
	assert( $v13 is Function )
	assert( $v13 == repr )
	
	let $v14 = nothing or function(){}
	assert( $v14 is Function )
	assert( $v14 is Function )
	
	let $v15 = nothing or Integer
	assert( $v15 is Type )
	
	let $v16 = nothing or Type
	assert( $v16 is Type )
	
	let $v17 = nothing or types
	assert( $v17 is Module )
	assert( $v17 == types )

$ $BURN or_result_1.burn

or_result_2.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = true or nothing
	assert( $v0 is Boolean )
	assert( $v0 == true )
	
	let $v1 = true or true
	assert( $v1 is Boolean )
	assert( $v1 == true )
	
	let $v2 = true or false
	assert( $v2 is Boolean )
	assert( $v2 == true )
	
	let $v3 = true or 0
	assert( $v3 is Boolean )
	assert( $v3 == true )
	
	let $v4 = true or 2
	assert( $v4 is Boolean )
	assert( $v4 == true )
	
	let $v5 = true or -2
	assert( $v5 is Boolean )
	assert( $v5 == true )
	
	let $v6 = true or 0.0
	assert( $v6 is Boolean )
	assert( $v6 == true )
	
	let $v7 = true or 2.0
	assert( $v7 is Boolean )
	assert( $v7 == true )
	
	let $v8 = true or -0.5
	assert( $v8 is Boolean )
	assert( $v8 == true )
	
	let $v9 = true or "c"[0]
	assert( $v9 is Boolean )
	assert( $v9 == true )
	
	let $v10 = true or ""
	assert( $v10 is Boolean )
	assert( $v10 == true )
	
	let $v11 = true or "apple"
	assert( $v11 is Boolean )
	assert( $v11 == true )
	
	let $v12 = true or "banana"
	assert( $v12 is Boolean )
	assert( $v12 == true )
	
	let $v13 = true or repr
	assert( $v13 is Boolean )
	assert( $v13 == true )
	
	let $v14 = true or function(){}
	assert( $v14 is Boolean )
	assert( $v14 == true )
	
	let $v15 = true or Integer
	assert( $v15 is Boolean )
	assert( $v15 == true )
	
	let $v16 = true or Type
	assert( $v16 is Boolean )
	assert( $v16 == true )
	
	let $v17 = true or types
	assert( $v17 is Boolean )
	assert( $v17 == true )

$ $BURN or_result_2.burn

or_result_3.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = false or nothing
	assert( $v0 is Nothing )
	
	let $v1 = false or true
	assert( $v1 is Boolean )
	assert( $v1 == true )
	
	let $v2 = false or false
	assert( $v2 is Boolean )
	assert( $v2 == false )
	
	let $v3 = false or 0
	assert( $v3 is Integer )
	assert( $v3 == 0 )
	
	let $v4 = false or 2
	assert( $v4 is Integer )
	assert( $v4 == 2 )
	
	let $v5 = false or -2
	assert( $v5 is Integer )
	assert( $v5 == -2 )
	
	let $v6 = false or 0.0
	assert( $v6 is Float )
	assert( ( -0.0001 < $v6 ) and ( $v6 < 0.0001 ) )
	
	let $v7 = false or 2.0
	assert( $v7 is Float )
	assert( ( 1.9999 < $v7 ) and ( $v7 < 2.0001 ) )
	
	let $v8 = false or -0.5
	assert( $v8 is Float )
	assert( ( -0.5001 < $v8 ) and ( $v8 < -0.4999 ) )
	
	let $v9 = false or "c"[0]
	assert( $v9 is Character )
	assert( $v9 == "c"[0] )
	
	let $v10 = false or ""
	assert( $v10 is String )
	assert( $v10 == "" )
	
	let $v11 = false or "apple"
	assert( $v11 is String )
	assert( $v11 == "apple" )
	
	let $v12 = false or "banana"
	assert( $v12 is String )
	assert( $v12 == "banana" )
	
	let $v13 = false or repr
	assert( $v13 is Function )
	assert( $v13 == repr )
	
	let $v14 = false or function(){}
	assert( $v14 is Function )
	assert( $v14 is Function )
	
	let $v15 = false or Integer
	assert( $v15 is Type )
	
	let $v16 = false or Type
	assert( $v16 is Type )
	
	let $v17 = false or types
	assert( $v17 is Module )
	assert( $v17 == types )

$ $BURN or_result_3.burn

or_result_4.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = 0 or nothing
	assert( $v0 is Nothing )
	
	let $v1 = 0 or true
	assert( $v1 is Boolean )
	assert( $v1 == true )
	
	let $v2 = 0 or false
	assert( $v2 is Boolean )
	assert( $v2 == false )
	
	let $v3 = 0 or 0
	assert( $v3 is Integer )
	assert( $v3 == 0 )
	
	let $v4 = 0 or 2
	assert( $v4 is Integer )
	assert( $v4 == 2 )
	
	let $v5 = 0 or -2
	assert( $v5 is Integer )
	assert( $v5 == -2 )
	
	let $v6 = 0 or 0.0
	assert( $v6 is Float )
	assert( ( -0.0001 < $v6 ) and ( $v6 < 0.0001 ) )
	
	let $v7 = 0 or 2.0
	assert( $v7 is Float )
	assert( ( 1.9999 < $v7 ) and ( $v7 < 2.0001 ) )
	
	let $v8 = 0 or -0.5
	assert( $v8 is Float )
	assert( ( -0.5001 < $v8 ) and ( $v8 < -0.4999 ) )
	
	let $v9 = 0 or "c"[0]
	assert( $v9 is Character )
	assert( $v9 == "c"[0] )
	
	let $v10 = 0 or ""
	assert( $v10 is String )
	assert( $v10 == "" )
	
	let $v11 = 0 or "apple"
	assert( $v11 is String )
	assert( $v11 == "apple" )
	
	let $v12 = 0 or "banana"
	assert( $v12 is String )
	assert( $v12 == "banana" )
	
	let $v13 = 0 or repr
	assert( $v13 is Function )
	assert( $v13 == repr )
	
	let $v14 = 0 or function(){}
	assert( $v14 is Function )
	assert( $v14 is Function )
	
	let $v15 = 0 or Integer
	assert( $v15 is Type )
	
	let $v16 = 0 or Type
	assert( $v16 is Type )
	
	let $v17 = 0 or types
	assert( $v17 is Module )
	assert( $v17 == types )

$ $BURN or_result_4.burn

or_result_5.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = 2 or nothing
	assert( $v0 is Integer )
	assert( $v0 == 2 )
	
	let $v1 = 2 or true
	assert( $v1 is Integer )
	assert( $v1 == 2 )
	
	let $v2 = 2 or false
	assert( $v2 is Integer )
	assert( $v2 == 2 )
	
	let $v3 = 2 or 0
	assert( $v3 is Integer )
	assert( $v3 == 2 )
	
	let $v4 = 2 or 2
	assert( $v4 is Integer )
	assert( $v4 == 2 )
	
	let $v5 = 2 or -2
	assert( $v5 is Integer )
	assert( $v5 == 2 )
	
	let $v6 = 2 or 0.0
	assert( $v6 is Integer )
	assert( $v6 == 2 )
	
	let $v7 = 2 or 2.0
	assert( $v7 is Integer )
	assert( $v7 == 2 )
	
	let $v8 = 2 or -0.5
	assert( $v8 is Integer )
	assert( $v8 == 2 )
	
	let $v9 = 2 or "c"[0]
	assert( $v9 is Integer )
	assert( $v9 == 2 )
	
	let $v10 = 2 or ""
	assert( $v10 is Integer )
	assert( $v10 == 2 )
	
	let $v11 = 2 or "apple"
	assert( $v11 is Integer )
	assert( $v11 == 2 )
	
	let $v12 = 2 or "banana"
	assert( $v12 is Integer )
	assert( $v12 == 2 )
	
	let $v13 = 2 or repr
	assert( $v13 is Integer )
	assert( $v13 == 2 )
	
	let $v14 = 2 or function(){}
	assert( $v14 is Integer )
	assert( $v14 == 2 )
	
	let $v15 = 2 or Integer
	assert( $v15 is Integer )
	assert( $v15 == 2 )
	
	let $v16 = 2 or Type
	assert( $v16 is Integer )
	assert( $v16 == 2 )
	
	let $v17 = 2 or types
	assert( $v17 is Integer )
	assert( $v17 == 2 )

$ $BURN or_result_5.burn

or_result_6.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = -2 or nothing
	assert( $v0 is Integer )
	assert( $v0 == -2 )
	
	let $v1 = -2 or true
	assert( $v1 is Integer )
	assert( $v1 == -2 )
	
	let $v2 = -2 or false
	assert( $v2 is Integer )
	assert( $v2 == -2 )
	
	let $v3 = -2 or 0
	assert( $v3 is Integer )
	assert( $v3 == -2 )
	
	let $v4 = -2 or 2
	assert( $v4 is Integer )
	assert( $v4 == -2 )
	
	let $v5 = -2 or -2
	assert( $v5 is Integer )
	assert( $v5 == -2 )
	
	let $v6 = -2 or 0.0
	assert( $v6 is Integer )
	assert( $v6 == -2 )
	
	let $v7 = -2 or 2.0
	assert( $v7 is Integer )
	assert( $v7 == -2 )
	
	let $v8 = -2 or -0.5
	assert( $v8 is Integer )
	assert( $v8 == -2 )
	
	let $v9 = -2 or "c"[0]
	assert( $v9 is Integer )
	assert( $v9 == -2 )
	
	let $v10 = -2 or ""
	assert( $v10 is Integer )
	assert( $v10 == -2 )
	
	let $v11 = -2 or "apple"
	assert( $v11 is Integer )
	assert( $v11 == -2 )
	
	let $v12 = -2 or "banana"
	assert( $v12 is Integer )
	assert( $v12 == -2 )
	
	let $v13 = -2 or repr
	assert( $v13 is Integer )
	assert( $v13 == -2 )
	
	let $v14 = -2 or function(){}
	assert( $v14 is Integer )
	assert( $v14 == -2 )
	
	let $v15 = -2 or Integer
	assert( $v15 is Integer )
	assert( $v15 == -2 )
	
	let $v16 = -2 or Type
	assert( $v16 is Integer )
	assert( $v16 == -2 )
	
	let $v17 = -2 or types
	assert( $v17 is Integer )
	assert( $v17 == -2 )

$ $BURN or_result_6.burn

or_result_7.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = 0.0 or nothing
	assert( $v0 is Nothing )
	
	let $v1 = 0.0 or true
	assert( $v1 is Boolean )
	assert( $v1 == true )
	
	let $v2 = 0.0 or false
	assert( $v2 is Boolean )
	assert( $v2 == false )
	
	let $v3 = 0.0 or 0
	assert( $v3 is Integer )
	assert( $v3 == 0 )
	
	let $v4 = 0.0 or 2
	assert( $v4 is Integer )
	assert( $v4 == 2 )
	
	let $v5 = 0.0 or -2
	assert( $v5 is Integer )
	assert( $v5 == -2 )
	
	let $v6 = 0.0 or 0.0
	assert( $v6 is Float )
	assert( ( -0.0001 < $v6 ) and ( $v6 < 0.0001 ) )
	
	let $v7 = 0.0 or 2.0
	assert( $v7 is Float )
	assert( ( 1.9999 < $v7 ) and ( $v7 < 2.0001 ) )
	
	let $v8 = 0.0 or -0.5
	assert( $v8 is Float )
	assert( ( -0.5001 < $v8 ) and ( $v8 < -0.4999 ) )
	
	let $v9 = 0.0 or "c"[0]
	assert( $v9 is Character )
	assert( $v9 == "c"[0] )
	
	let $v10 = 0.0 or ""
	assert( $v10 is String )
	assert( $v10 == "" )
	
	let $v11 = 0.0 or "apple"
	assert( $v11 is String )
	assert( $v11 == "apple" )
	
	let $v12 = 0.0 or "banana"
	assert( $v12 is String )
	assert( $v12 == "banana" )
	
	let $v13 = 0.0 or repr
	assert( $v13 is Function )
	assert( $v13 == repr )
	
	let $v14 = 0.0 or function(){}
	assert( $v14 is Function )
	assert( $v14 is Function )
	
	let $v15 = 0.0 or Integer
	assert( $v15 is Type )
	
	let $v16 = 0.0 or Type
	assert( $v16 is Type )
	
	let $v17 = 0.0 or types
	assert( $v17 is Module )
	assert( $v17 == types )

$ $BURN or_result_7.burn

or_result_8.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = 2.0 or nothing
	assert( $v0 is Float )
	assert( ( 1.9999 < $v0 ) and ( $v0 < 2.0001 ) )
	
	let $v1 = 2.0 or true
	assert( $v1 is Float )
	assert( ( 1.9999 < $v1 ) and ( $v1 < 2.0001 ) )
	
	let $v2 = 2.0 or false
	assert( $v2 is Float )
	assert( ( 1.9999 < $v2 ) and ( $v2 < 2.0001 ) )
	
	let $v3 = 2.0 or 0
	assert( $v3 is Float )
	assert( ( 1.9999 < $v3 ) and ( $v3 < 2.0001 ) )
	
	let $v4 = 2.0 or 2
	assert( $v4 is Float )
	assert( ( 1.9999 < $v4 ) and ( $v4 < 2.0001 ) )
	
	let $v5 = 2.0 or -2
	assert( $v5 is Float )
	assert( ( 1.9999 < $v5 ) and ( $v5 < 2.0001 ) )
	
	let $v6 = 2.0 or 0.0
	assert( $v6 is Float )
	assert( ( 1.9999 < $v6 ) and ( $v6 < 2.0001 ) )
	
	let $v7 = 2.0 or 2.0
	assert( $v7 is Float )
	assert( ( 1.9999 < $v7 ) and ( $v7 < 2.0001 ) )
	
	let $v8 = 2.0 or -0.5
	assert( $v8 is Float )
	assert( ( 1.9999 < $v8 ) and ( $v8 < 2.0001 ) )
	
	let $v9 = 2.0 or "c"[0]
	assert( $v9 is Float )
	assert( ( 1.9999 < $v9 ) and ( $v9 < 2.0001 ) )
	
	let $v10 = 2.0 or ""
	assert( $v10 is Float )
	assert( ( 1.9999 < $v10 ) and ( $v10 < 2.0001 ) )
	
	let $v11 = 2.0 or "apple"
	assert( $v11 is Float )
	assert( ( 1.9999 < $v11 ) and ( $v11 < 2.0001 ) )
	
	let $v12 = 2.0 or "banana"
	assert( $v12 is Float )
	assert( ( 1.9999 < $v12 ) and ( $v12 < 2.0001 ) )
	
	let $v13 = 2.0 or repr
	assert( $v13 is Float )
	assert( ( 1.9999 < $v13 ) and ( $v13 < 2.0001 ) )
	
	let $v14 = 2.0 or function(){}
	assert( $v14 is Float )
	assert( ( 1.9999 < $v14 ) and ( $v14 < 2.0001 ) )
	
	let $v15 = 2.0 or Integer
	assert( $v15 is Float )
	assert( ( 1.9999 < $v15 ) and ( $v15 < 2.0001 ) )
	
	let $v16 = 2.0 or Type
	assert( $v16 is Float )
	assert( ( 1.9999 < $v16 ) and ( $v16 < 2.0001 ) )
	
	let $v17 = 2.0 or types
	assert( $v17 is Float )
	assert( ( 1.9999 < $v17 ) and ( $v17 < 2.0001 ) )

$ $BURN or_result_8.burn

or_result_9.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = -0.5 or nothing
	assert( $v0 is Float )
	assert( ( -0.5001 < $v0 ) and ( $v0 < -0.4999 ) )
	
	let $v1 = -0.5 or true
	assert( $v1 is Float )
	assert( ( -0.5001 < $v1 ) and ( $v1 < -0.4999 ) )
	
	let $v2 = -0.5 or false
	assert( $v2 is Float )
	assert( ( -0.5001 < $v2 ) and ( $v2 < -0.4999 ) )
	
	let $v3 = -0.5 or 0
	assert( $v3 is Float )
	assert( ( -0.5001 < $v3 ) and ( $v3 < -0.4999 ) )
	
	let $v4 = -0.5 or 2
	assert( $v4 is Float )
	assert( ( -0.5001 < $v4 ) and ( $v4 < -0.4999 ) )
	
	let $v5 = -0.5 or -2
	assert( $v5 is Float )
	assert( ( -0.5001 < $v5 ) and ( $v5 < -0.4999 ) )
	
	let $v6 = -0.5 or 0.0
	assert( $v6 is Float )
	assert( ( -0.5001 < $v6 ) and ( $v6 < -0.4999 ) )
	
	let $v7 = -0.5 or 2.0
	assert( $v7 is Float )
	assert( ( -0.5001 < $v7 ) and ( $v7 < -0.4999 ) )
	
	let $v8 = -0.5 or -0.5
	assert( $v8 is Float )
	assert( ( -0.5001 < $v8 ) and ( $v8 < -0.4999 ) )
	
	let $v9 = -0.5 or "c"[0]
	assert( $v9 is Float )
	assert( ( -0.5001 < $v9 ) and ( $v9 < -0.4999 ) )
	
	let $v10 = -0.5 or ""
	assert( $v10 is Float )
	assert( ( -0.5001 < $v10 ) and ( $v10 < -0.4999 ) )
	
	let $v11 = -0.5 or "apple"
	assert( $v11 is Float )
	assert( ( -0.5001 < $v11 ) and ( $v11 < -0.4999 ) )
	
	let $v12 = -0.5 or "banana"
	assert( $v12 is Float )
	assert( ( -0.5001 < $v12 ) and ( $v12 < -0.4999 ) )
	
	let $v13 = -0.5 or repr
	assert( $v13 is Float )
	assert( ( -0.5001 < $v13 ) and ( $v13 < -0.4999 ) )
	
	let $v14 = -0.5 or function(){}
	assert( $v14 is Float )
	assert( ( -0.5001 < $v14 ) and ( $v14 < -0.4999 ) )
	
	let $v15 = -0.5 or Integer
	assert( $v15 is Float )
	assert( ( -0.5001 < $v15 ) and ( $v15 < -0.4999 ) )
	
	let $v16 = -0.5 or Type
	assert( $v16 is Float )
	assert( ( -0.5001 < $v16 ) and ( $v16 < -0.4999 ) )
	
	let $v17 = -0.5 or types
	assert( $v17 is Float )
	assert( ( -0.5001 < $v17 ) and ( $v17 < -0.4999 ) )

$ $BURN or_result_9.burn

or_result_10.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = "c"[0] or nothing
	assert( $v0 is Character )
	assert( $v0 == "c"[0] )
	
	let $v1 = "c"[0] or true
	assert( $v1 is Character )
	assert( $v1 == "c"[0] )
	
	let $v2 = "c"[0] or false
	assert( $v2 is Character )
	assert( $v2 == "c"[0] )
	
	let $v3 = "c"[0] or 0
	assert( $v3 is Character )
	assert( $v3 == "c"[0] )
	
	let $v4 = "c"[0] or 2
	assert( $v4 is Character )
	assert( $v4 == "c"[0] )
	
	let $v5 = "c"[0] or -2
	assert( $v5 is Character )
	assert( $v5 == "c"[0] )
	
	let $v6 = "c"[0] or 0.0
	assert( $v6 is Character )
	assert( $v6 == "c"[0] )
	
	let $v7 = "c"[0] or 2.0
	assert( $v7 is Character )
	assert( $v7 == "c"[0] )
	
	let $v8 = "c"[0] or -0.5
	assert( $v8 is Character )
	assert( $v8 == "c"[0] )
	
	let $v9 = "c"[0] or "c"[0]
	assert( $v9 is Character )
	assert( $v9 == "c"[0] )
	
	let $v10 = "c"[0] or ""
	assert( $v10 is Character )
	assert( $v10 == "c"[0] )
	
	let $v11 = "c"[0] or "apple"
	assert( $v11 is Character )
	assert( $v11 == "c"[0] )
	
	let $v12 = "c"[0] or "banana"
	assert( $v12 is Character )
	assert( $v12 == "c"[0] )
	
	let $v13 = "c"[0] or repr
	assert( $v13 is Character )
	assert( $v13 == "c"[0] )
	
	let $v14 = "c"[0] or function(){}
	assert( $v14 is Character )
	assert( $v14 == "c"[0] )
	
	let $v15 = "c"[0] or Integer
	assert( $v15 is Character )
	assert( $v15 == "c"[0] )
	
	let $v16 = "c"[0] or Type
	assert( $v16 is Character )
	assert( $v16 == "c"[0] )
	
	let $v17 = "c"[0] or types
	assert( $v17 is Character )
	assert( $v17 == "c"[0] )

$ $BURN or_result_10.burn

or_result_11.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = "" or nothing
	assert( $v0 is Nothing )
	
	let $v1 = "" or true
	assert( $v1 is Boolean )
	assert( $v1 == true )
	
	let $v2 = "" or false
	assert( $v2 is Boolean )
	assert( $v2 == false )
	
	let $v3 = "" or 0
	assert( $v3 is Integer )
	assert( $v3 == 0 )
	
	let $v4 = "" or 2
	assert( $v4 is Integer )
	assert( $v4 == 2 )
	
	let $v5 = "" or -2
	assert( $v5 is Integer )
	assert( $v5 == -2 )
	
	let $v6 = "" or 0.0
	assert( $v6 is Float )
	assert( ( -0.0001 < $v6 ) and ( $v6 < 0.0001 ) )
	
	let $v7 = "" or 2.0
	assert( $v7 is Float )
	assert( ( 1.9999 < $v7 ) and ( $v7 < 2.0001 ) )
	
	let $v8 = "" or -0.5
	assert( $v8 is Float )
	assert( ( -0.5001 < $v8 ) and ( $v8 < -0.4999 ) )
	
	let $v9 = "" or "c"[0]
	assert( $v9 is Character )
	assert( $v9 == "c"[0] )
	
	let $v10 = "" or ""
	assert( $v10 is String )
	assert( $v10 == "" )
	
	let $v11 = "" or "apple"
	assert( $v11 is String )
	assert( $v11 == "apple" )
	
	let $v12 = "" or "banana"
	assert( $v12 is String )
	assert( $v12 == "banana" )
	
	let $v13 = "" or repr
	assert( $v13 is Function )
	assert( $v13 == repr )
	
	let $v14 = "" or function(){}
	assert( $v14 is Function )
	assert( $v14 is Function )
	
	let $v15 = "" or Integer
	assert( $v15 is Type )
	
	let $v16 = "" or Type
	assert( $v16 is Type )
	
	let $v17 = "" or types
	assert( $v17 is Module )
	assert( $v17 == types )

$ $BURN or_result_11.burn

or_result_12.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = "apple" or nothing
	assert( $v0 is String )
	assert( $v0 == "apple" )
	
	let $v1 = "apple" or true
	assert( $v1 is String )
	assert( $v1 == "apple" )
	
	let $v2 = "apple" or false
	assert( $v2 is String )
	assert( $v2 == "apple" )
	
	let $v3 = "apple" or 0
	assert( $v3 is String )
	assert( $v3 == "apple" )
	
	let $v4 = "apple" or 2
	assert( $v4 is String )
	assert( $v4 == "apple" )
	
	let $v5 = "apple" or -2
	assert( $v5 is String )
	assert( $v5 == "apple" )
	
	let $v6 = "apple" or 0.0
	assert( $v6 is String )
	assert( $v6 == "apple" )
	
	let $v7 = "apple" or 2.0
	assert( $v7 is String )
	assert( $v7 == "apple" )
	
	let $v8 = "apple" or -0.5
	assert( $v8 is String )
	assert( $v8 == "apple" )
	
	let $v9 = "apple" or "c"[0]
	assert( $v9 is String )
	assert( $v9 == "apple" )
	
	let $v10 = "apple" or ""
	assert( $v10 is String )
	assert( $v10 == "apple" )
	
	let $v11 = "apple" or "apple"
	assert( $v11 is String )
	assert( $v11 == "apple" )
	
	let $v12 = "apple" or "banana"
	assert( $v12 is String )
	assert( $v12 == "apple" )
	
	let $v13 = "apple" or repr
	assert( $v13 is String )
	assert( $v13 == "apple" )
	
	let $v14 = "apple" or function(){}
	assert( $v14 is String )
	assert( $v14 == "apple" )
	
	let $v15 = "apple" or Integer
	assert( $v15 is String )
	assert( $v15 == "apple" )
	
	let $v16 = "apple" or Type
	assert( $v16 is String )
	assert( $v16 == "apple" )
	
	let $v17 = "apple" or types
	assert( $v17 is String )
	assert( $v17 == "apple" )

$ $BURN or_result_12.burn

or_result_13.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = "banana" or nothing
	assert( $v0 is String )
	assert( $v0 == "banana" )
	
	let $v1 = "banana" or true
	assert( $v1 is String )
	assert( $v1 == "banana" )
	
	let $v2 = "banana" or false
	assert( $v2 is String )
	assert( $v2 == "banana" )
	
	let $v3 = "banana" or 0
	assert( $v3 is String )
	assert( $v3 == "banana" )
	
	let $v4 = "banana" or 2
	assert( $v4 is String )
	assert( $v4 == "banana" )
	
	let $v5 = "banana" or -2
	assert( $v5 is String )
	assert( $v5 == "banana" )
	
	let $v6 = "banana" or 0.0
	assert( $v6 is String )
	assert( $v6 == "banana" )
	
	let $v7 = "banana" or 2.0
	assert( $v7 is String )
	assert( $v7 == "banana" )
	
	let $v8 = "banana" or -0.5
	assert( $v8 is String )
	assert( $v8 == "banana" )
	
	let $v9 = "banana" or "c"[0]
	assert( $v9 is String )
	assert( $v9 == "banana" )
	
	let $v10 = "banana" or ""
	assert( $v10 is String )
	assert( $v10 == "banana" )
	
	let $v11 = "banana" or "apple"
	assert( $v11 is String )
	assert( $v11 == "banana" )
	
	let $v12 = "banana" or "banana"
	assert( $v12 is String )
	assert( $v12 == "banana" )
	
	let $v13 = "banana" or repr
	assert( $v13 is String )
	assert( $v13 == "banana" )
	
	let $v14 = "banana" or function(){}
	assert( $v14 is String )
	assert( $v14 == "banana" )
	
	let $v15 = "banana" or Integer
	assert( $v15 is String )
	assert( $v15 == "banana" )
	
	let $v16 = "banana" or Type
	assert( $v16 is String )
	assert( $v16 == "banana" )
	
	let $v17 = "banana" or types
	assert( $v17 is String )
	assert( $v17 == "banana" )

$ $BURN or_result_13.burn

or_result_14.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = repr or nothing
	assert( $v0 is Function )
	assert( $v0 == repr )
	
	let $v1 = repr or true
	assert( $v1 is Function )
	assert( $v1 == repr )
	
	let $v2 = repr or false
	assert( $v2 is Function )
	assert( $v2 == repr )
	
	let $v3 = repr or 0
	assert( $v3 is Function )
	assert( $v3 == repr )
	
	let $v4 = repr or 2
	assert( $v4 is Function )
	assert( $v4 == repr )
	
	let $v5 = repr or -2
	assert( $v5 is Function )
	assert( $v5 == repr )
	
	let $v6 = repr or 0.0
	assert( $v6 is Function )
	assert( $v6 == repr )
	
	let $v7 = repr or 2.0
	assert( $v7 is Function )
	assert( $v7 == repr )
	
	let $v8 = repr or -0.5
	assert( $v8 is Function )
	assert( $v8 == repr )
	
	let $v9 = repr or "c"[0]
	assert( $v9 is Function )
	assert( $v9 == repr )
	
	let $v10 = repr or ""
	assert( $v10 is Function )
	assert( $v10 == repr )
	
	let $v11 = repr or "apple"
	assert( $v11 is Function )
	assert( $v11 == repr )
	
	let $v12 = repr or "banana"
	assert( $v12 is Function )
	assert( $v12 == repr )
	
	let $v13 = repr or repr
	assert( $v13 is Function )
	assert( $v13 == repr )
	
	let $v14 = repr or function(){}
	assert( $v14 is Function )
	assert( $v14 == repr )
	
	let $v15 = repr or Integer
	assert( $v15 is Function )
	assert( $v15 == repr )
	
	let $v16 = repr or Type
	assert( $v16 is Function )
	assert( $v16 == repr )
	
	let $v17 = repr or types
	assert( $v17 is Function )
	assert( $v17 == repr )

$ $BURN or_result_14.burn

or_result_15.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = function(){} or nothing
	assert( $v0 is Function )
	assert( $v0 is Function )
	
	let $v1 = function(){} or true
	assert( $v1 is Function )
	assert( $v1 is Function )
	
	let $v2 = function(){} or false
	assert( $v2 is Function )
	assert( $v2 is Function )
	
	let $v3 = function(){} or 0
	assert( $v3 is Function )
	assert( $v3 is Function )
	
	let $v4 = function(){} or 2
	assert( $v4 is Function )
	assert( $v4 is Function )
	
	let $v5 = function(){} or -2
	assert( $v5 is Function )
	assert( $v5 is Function )
	
	let $v6 = function(){} or 0.0
	assert( $v6 is Function )
	assert( $v6 is Function )
	
	let $v7 = function(){} or 2.0
	assert( $v7 is Function )
	assert( $v7 is Function )
	
	let $v8 = function(){} or -0.5
	assert( $v8 is Function )
	assert( $v8 is Function )
	
	let $v9 = function(){} or "c"[0]
	assert( $v9 is Function )
	assert( $v9 is Function )
	
	let $v10 = function(){} or ""
	assert( $v10 is Function )
	assert( $v10 is Function )
	
	let $v11 = function(){} or "apple"
	assert( $v11 is Function )
	assert( $v11 is Function )
	
	let $v12 = function(){} or "banana"
	assert( $v12 is Function )
	assert( $v12 is Function )
	
	let $v13 = function(){} or repr
	assert( $v13 is Function )
	assert( $v13 is Function )
	
	let $v14 = function(){} or function(){}
	assert( $v14 is Function )
	assert( $v14 is Function )
	
	let $v15 = function(){} or Integer
	assert( $v15 is Function )
	assert( $v15 is Function )
	
	let $v16 = function(){} or Type
	assert( $v16 is Function )
	assert( $v16 is Function )
	
	let $v17 = function(){} or types
	assert( $v17 is Function )
	assert( $v17 is Function )

$ $BURN or_result_15.burn

or_result_16.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = Integer or nothing
	assert( $v0 is Type )
	
	let $v1 = Integer or true
	assert( $v1 is Type )
	
	let $v2 = Integer or false
	assert( $v2 is Type )
	
	let $v3 = Integer or 0
	assert( $v3 is Type )
	
	let $v4 = Integer or 2
	assert( $v4 is Type )
	
	let $v5 = Integer or -2
	assert( $v5 is Type )
	
	let $v6 = Integer or 0.0
	assert( $v6 is Type )
	
	let $v7 = Integer or 2.0
	assert( $v7 is Type )
	
	let $v8 = Integer or -0.5
	assert( $v8 is Type )
	
	let $v9 = Integer or "c"[0]
	assert( $v9 is Type )
	
	let $v10 = Integer or ""
	assert( $v10 is Type )
	
	let $v11 = Integer or "apple"
	assert( $v11 is Type )
	
	let $v12 = Integer or "banana"
	assert( $v12 is Type )
	
	let $v13 = Integer or repr
	assert( $v13 is Type )
	
	let $v14 = Integer or function(){}
	assert( $v14 is Type )
	
	let $v15 = Integer or Integer
	assert( $v15 is Type )
	
	let $v16 = Integer or Type
	assert( $v16 is Type )
	
	let $v17 = Integer or types
	assert( $v17 is Type )

$ $BURN or_result_16.burn

or_result_17.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = Type or nothing
	assert( $v0 is Type )
	
	let $v1 = Type or true
	assert( $v1 is Type )
	
	let $v2 = Type or false
	assert( $v2 is Type )
	
	let $v3 = Type or 0
	assert( $v3 is Type )
	
	let $v4 = Type or 2
	assert( $v4 is Type )
	
	let $v5 = Type or -2
	assert( $v5 is Type )
	
	let $v6 = Type or 0.0
	assert( $v6 is Type )
	
	let $v7 = Type or 2.0
	assert( $v7 is Type )
	
	let $v8 = Type or -0.5
	assert( $v8 is Type )
	
	let $v9 = Type or "c"[0]
	assert( $v9 is Type )
	
	let $v10 = Type or ""
	assert( $v10 is Type )
	
	let $v11 = Type or "apple"
	assert( $v11 is Type )
	
	let $v12 = Type or "banana"
	assert( $v12 is Type )
	
	let $v13 = Type or repr
	assert( $v13 is Type )
	
	let $v14 = Type or function(){}
	assert( $v14 is Type )
	
	let $v15 = Type or Integer
	assert( $v15 is Type )
	
	let $v16 = Type or Type
	assert( $v16 is Type )
	
	let $v17 = Type or types
	assert( $v17 is Type )

$ $BURN or_result_17.burn

or_result_18.burn:
	import test
	import burn.types
	import burn.errors
	
	let $v0 = types or nothing
	assert( $v0 is Module )
	assert( $v0 == types )
	
	let $v1 = types or true
	assert( $v1 is Module )
	assert( $v1 == types )
	
	let $v2 = types or false
	assert( $v2 is Module )
	assert( $v2 == types )
	
	let $v3 = types or 0
	assert( $v3 is Module )
	assert( $v3 == types )
	
	let $v4 = types or 2
	assert( $v4 is Module )
	assert( $v4 == types )
	
	let $v5 = types or -2
	assert( $v5 is Module )
	assert( $v5 == types )
	
	let $v6 = types or 0.0
	assert( $v6 is Module )
	assert( $v6 == types )
	
	let $v7 = types or 2.0
	assert( $v7 is Module )
	assert( $v7 == types )
	
	let $v8 = types or -0.5
	assert( $v8 is Module )
	assert( $v8 == types )
	
	let $v9 = types or "c"[0]
	assert( $v9 is Module )
	assert( $v9 == types )
	
	let $v10 = types or ""
	assert( $v10 is Module )
	assert( $v10 == types )
	
	let $v11 = types or "apple"
	assert( $v11 is Module )
	assert( $v11 == types )
	
	let $v12 = types or "banana"
	assert( $v12 is Module )
	assert( $v12 == types )
	
	let $v13 = types or repr
	assert( $v13 is Module )
	assert( $v13 == types )
	
	let $v14 = types or function(){}
	assert( $v14 is Module )
	assert( $v14 == types )
	
	let $v15 = types or Integer
	assert( $v15 is Module )
	assert( $v15 == types )
	
	let $v16 = types or Type
	assert( $v16 is Module )
	assert( $v16 == types )
	
	let $v17 = types or types
	assert( $v17 is Module )
	assert( $v17 == types )

$ $BURN or_result_18.burn
