// generated by `etc/generate_operator_test.py result is`

is_result_1.burn:
	let $r13 = nothing is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_1.burn
	* stdout
		<Boolean>
		false

is_result_2.burn:
	let $r13 = true is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_2.burn
	* stdout
		<Boolean>
		true

is_result_3.burn:
	let $r13 = false is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_3.burn
	* stdout
		<Boolean>
		true

is_result_4.burn:
	let $r13 = 0 is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_4.burn
	* stdout
		<Boolean>
		true

is_result_5.burn:
	let $r13 = 2 is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_5.burn
	* stdout
		<Boolean>
		true

is_result_6.burn:
	let $r13 = -2 is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_6.burn
	* stdout
		<Boolean>
		true

is_result_7.burn:
	let $r13 = 0.0 is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_7.burn
	* stdout
		<Boolean>
		true

is_result_8.burn:
	let $r13 = 2.0 is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_8.burn
	* stdout
		<Boolean>
		true

is_result_9.burn:
	let $r13 = -0.5 is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_9.burn
	* stdout
		<Boolean>
		true

is_result_10.burn:
	let $r13 = "" is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_10.burn
	* stdout
		<Boolean>
		true

is_result_11.burn:
	let $r13 = "foo" is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_11.burn
	* stdout
		<Boolean>
		true

is_result_12.burn:
	let $r13 = function(){} is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_12.burn
	* stdout
		<Boolean>
		true

is_result_13.burn:
	let $r13 = Something is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_13.burn
	* stdout
		<Boolean>
		true

is_result_14.burn:
	import burn
	let $r13 = burn is Something
	print repr( $r13 )
	print $r13

$ burn --tolerant is_result_14.burn
	* stdout
		<Boolean>
		true
